[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"


[project]
name = "esgf-publisher-workflow"
version = "0.1.0"

dependencies = [
    "duckdb",
    "pyyaml",
    "typer"
]


[tool.setuptools.packages.find]
include = [
    "workflow*"
]